# Model Configuration

# LLM Settings - OpenRouter API
llm:
  provider: "openrouter"
  base_url: "https://openrouter.ai/api/v1"
  api_key: "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"
  model_name: "meta-llama/llama-3.1-8b-instruct:free"  # Free model, can be changed
  temperature: 0.7
  max_tokens: 2048
  top_p: 0.9
  
# Embedding Model Settings - Sentence Transformers
embeddings:
  provider: "sentence-transformers"
  model_name: "all-MiniLM-L6-v2"  # Fast and efficient, 384 dimensions
  dimension: 384
  device: "cpu"  # Options: cpu, cuda
  
# Alternative Embedding Models (uncomment to use)
# embeddings:
#   model_name: "all-mpnet-base-v2"  # Higher quality, 768 dimensions
#   dimension: 768

# OpenRouter Specific Settings
openrouter:
  # Popular model options (update model_name above to use):
  # Free models:
  #   - "meta-llama/llama-3.1-8b-instruct:free"
  #   - "google/gemma-2-9b-it:free"
  #   - "mistralai/mistral-7b-instruct:free"
  # Paid models (better quality):
  #   - "anthropic/claude-3.5-sonnet"
  #   - "openai/gpt-4-turbo"
  #   - "google/gemini-pro-1.5"
  
  site_url: "https://your-app-url.com"  # Optional: for rankings
  app_name: "PDF-RAG-QA-System"  # Optional: for tracking
